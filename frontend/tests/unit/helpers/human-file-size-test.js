import {
  humanFileSize
} from 'docstore/helpers/human-file-size';

module('HumanFileSizeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = humanFileSize(42);
  ok(result);
});
