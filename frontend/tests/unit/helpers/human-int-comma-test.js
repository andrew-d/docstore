import {
  humanIntComma
} from 'docstore/helpers/human-int-comma';

module('HumanIntCommaHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = humanIntComma(42);
  ok(result);
});
