import {
  joinTags
} from 'docstore/helpers/join-tags';

module('JoinTagsHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = joinTags(42);
  ok(result);
});
