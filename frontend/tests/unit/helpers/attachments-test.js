import {
  attachments
} from 'docstore/helpers/attachments';

module('AttachmentsHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = attachments(42);
  ok(result);
});
