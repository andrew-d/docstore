import {
  momentCalendar
} from 'docstore/helpers/moment-calendar';

module('MomentCalendarHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = momentCalendar(42);
  ok(result);
});
