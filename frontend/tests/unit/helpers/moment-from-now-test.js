import {
  momentFromNow
} from 'docstore/helpers/moment-from-now';

module('MomentFromNowHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = momentFromNow(42);
  ok(result);
});
