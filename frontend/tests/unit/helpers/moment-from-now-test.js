import {
  momentFromNow
} from '../../../helpers/moment-from-now';
import { module, test } from 'qunit';

module('MomentFromNowHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = momentFromNow(42);
  assert.ok(result);
});
